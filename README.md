# PTPC Signaling Server

Signaling server for PTPC (Peer-to-Peer Chat) Android application using Socket.IO.

## Setup
```bash
npm install
npm start
```

## Environment Variables
- `PORT`: Server port (default: 3000)
